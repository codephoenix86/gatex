# Gatex

`gatex` is a concurrent API gateway written in Go. Phase 0 establishes its
configuration contract and package boundaries; the proxy, balancing, resilience,
and observability features are added in subsequent phases.

See [the architecture notes](docs/architecture.md) and
[the example configuration](configs/gateway.example.yaml) to get started.
